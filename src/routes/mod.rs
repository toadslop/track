//! The routing module is the meat of the application. It handles defining routes and
//! directing requests to the proper handler functions in the `crate::domain` module.

pub mod private;
pub mod public;
